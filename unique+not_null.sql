desc student_tops;
-- unique key+not null =primary key
-- unique key: no duplicate
-- one table can have more than one unique key

-- primary key: no duplicate,only one null value,
-- one primary key/table

insert into student_tops
(stu_name)values
("abbie");
